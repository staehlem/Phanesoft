package model;

public class GeneralUser extends User {
	public GeneralUser(String userName, String first, String last, String email,
			String password) {
		super(userName, first, last, email, password);
	}
}
