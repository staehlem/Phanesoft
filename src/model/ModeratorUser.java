package model;

public class ModeratorUser extends User {
	public ModeratorUser(String userName, String first, String last, String email,
			String password) {
		super(userName, first, last, email, password);
	}
}
