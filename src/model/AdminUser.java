package model;

public class AdminUser extends User {
	public AdminUser(String userName, String first, String last, String email,
			String password) {
		super(userName, first, last, email, password);
	}
}
